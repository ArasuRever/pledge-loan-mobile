name: pledge_loan_mobile
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  jwt_decode: ^0.3.1
  intl: ^0.19.0
  http: ^1.2.1
  shared_preferences: ^2.2.3
  image_picker: ^1.1.2
  permission_handler: ^11.3.1
  pdf: ^3.10.4
  printing: ^5.11.0
  url_launcher: ^6.2.5
  path_provider: ^2.1.2
  signature: ^5.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # --- Fixed: Now indented correctly as a sibling ---
  flutter_launcher_icons: ^0.13.1
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/sri_kubera_logo.png

# --- Launcher Icon Configuration ---
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  # Pointing this to your existing logo so it works immediately
  image_path: "assets/images/sri_kubera_logo.png"
  min_sdk_android: 21